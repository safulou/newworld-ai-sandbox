{
  "name": "newworld-ai-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "An open-source, browser-based voxel sandbox where prompts become playable worlds.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend"
  }
}
