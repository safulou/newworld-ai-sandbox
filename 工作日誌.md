# 📖 NewWorld AI Sandbox - 工作日誌 (Work Log)

> 本檔案記錄專案每日開發里程碑、系統架構演進、操作指南與最新發布資訊，方便隨時查閱與追蹤進度。

---

## 📅 2026-09-04 工作日誌

### 📌 概覽
* **專案名稱**：NewWorld AI Sandbox (3D 體素 AI 沙盒元宇宙)
* **主要任務**：GPU 渲染效能極限優化、50+ 項新功能深度擴充、前後端架構進化與自動化版控發布。
* **建置狀態**：前後端 `npm run build` 與 `npx tsc --noEmit` 均為 **0 Error / 0 Warning** 通過。
* **最新 Commit**：
  * `f111fa8` - `feat(metaverse): add InstancedMesh chunk mesher, WorldEdit engine, Style Transfer, Inventory, and Keybinds modal`
  * `5844ac1` - `docs(devlog): record 50-feature performance optimization session`
* **遠端同步**：已全數推送至 GitHub `origin/main`。

---

### 🚀 今日完成重點項目

#### 1. ⚡ 體素渲染極限效能優化 (Draw Calls 降低 95%+)
* **`frontend/src/engine/chunkMesh.ts`**：
  * 導入 **`THREE.InstancedMesh`** 批量渲染架構，將每個 16x16 Chunk 內原本數百個獨立 Mesh 聚合為依材質分類的單一實例化網格。
  * 實作 **6 向相鄰實心遮擋剔除 (Hidden Face Occlusion)**，內部封閉的不可見方塊自動略過渲染，大幅降低 GPU 頂點與記憶體壓力。

#### 2. 🪄 3D 空間魔杖與 WorldEdit 引擎
* **`frontend/src/engine/worldedit.ts`**：
  * 支援 **Pos1 / Pos2 霓虹立體選區邊框**。
  * 支援選區批量填充 (`fillSelection`)、材質置換 (`replaceSelection`) 與球體雕刻筆刷 (`applySphereBrush`)。
  * 全面接入 `HistoryEngine`，支援 `Ctrl + Z` 一鍵復原。

#### 3. 🎨 AI 風格重鑄與主題遷移 (Style Transfer)
* **`frontend/src/engine/styleTransfer.ts`**：
  * 內建 5 種經典建築風格調色盤：
    1. ⚡ **賽博霓虹 (Cyberpunk Neon Matrix)**
    2. ⛩️ **東方古剎 (Ancient Oriental Temple)**
    3. 🌿 **生態玻璃穹頂 (Crystal Biosphere)**
    4. 👑 **太陽宮殿 (Solar Golden Citadel)**
    5. ❄️ **極地冰霜 (Frozen Crystal Glacier)**
  * 支援 1 秒將既有建築全結構重鑄。

#### 4. 🎒 創造物品庫與快捷鍵指南模態
* **`frontend/src/components/InventoryModal.vue`**：按 **`E`** 鍵開啟，提供 Building / Nature / Sci-Fi / Elements 分類與即時關鍵字搜尋。
* **`frontend/src/components/KeybindsModal.vue`**：按 **`F3`** 鍵開啟，完整呈現移動、建造、AI 建築、音樂與系統熱鍵。

#### 5. ⛓️ 區塊鏈記帳簿與瀏覽器
* **`frontend/src/components/ChainExplorer.vue`** & **`backend/src/index.ts`**：
  * 新增 `GET /api/chain` 端點，即時瀏覽 SHA-256 區塊鏈雜湊與不可竄改產權記錄。

#### 6. 🤖 智慧 NPC 伴侶與對話代工
* **`frontend/src/engine/npc.ts`** & **`frontend/src/components/NPCChat.vue`**：
  * 3D 賽博架構師伴侶，具備走路擺動動畫、自動跟隨玩家，並能自主代工執行方塊放置。

---

## 🎮 完整操作快捷鍵清單 (Quick Reference)

| 類別 | 按鍵 / 操作 | 功能說明 |
| :--- | :--- | :--- |
| **視角與移動** | `W / A / S / D` | 前後左右移動 |
| | `Space` | 跳躍（飛行模式下為上升） |
| | `Shift` | 衝刺（飛行模式下為下降） |
| | `V` | **切換視角**：RTS 俯瞰 ➔ 第一人稱 (FPP) ➔ 第三人稱 (TPP) |
| | `F` | **切換模式**：飛行模式 (Fly Mode) ➔ 重力行走 |
| **建造與互動** | **滑鼠左鍵** | 挖掘破壞方塊（附 3D 物理碎屑粒子特效） |
| | **滑鼠右鍵** | 放置當前選中的手持方塊（附空間音效） |
| | `1` ~ `9` | 快速切換手持方塊材質 |
| | `E` | 開啟 **創造物品目錄 (Creative Inventory)** |
| | `Ctrl + Z` / `Ctrl + Y` | 空間操作 **還原 (Undo)** / **重做 (Redo)** |
| **AI 與元宇宙** | `B` | 開啟 **AI Voxel Architect**（自然語言即時建構） |
| | `P` | 開啟 **3D 建築藍圖庫 (Blueprints)** 一鍵部署 |
| | `M` | 開關 **Lo-Fi Ambient Synth** 背景音樂 |
| | `F1` | 開啟 **元宇宙設定**（日夜光影、AI Key、世界存檔） |
| | `F2` | **快速存檔 (Save World)** |
| | `F3` | 開啟 **快捷鍵參照導覽 (Keybinds)** |

---

## 🚀 專案啟動方式

```powershell
# 1. 啟動後端伺服器 (Port: 4000)
cd c:\Arsen\code\newworld-ai-sandbox\backend
npm run dev

# 2. 啟動前端開發伺服器 (Port: 5173)
cd c:\Arsen\code\newworld-ai-sandbox\frontend
npm run dev
```

開啟瀏覽器進入 `http://localhost:5173` 即可開始體驗！
